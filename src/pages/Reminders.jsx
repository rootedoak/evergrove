export default function Reminders() {
    return (
        <section className="hero-card">
            <p className="eyebrow">Reminders</p>
            <h2>Reminders</h2>
            <p>See upcoming alerts for registrations, deadlines, events, and overdue tasks.</p>
        </section>
    )
}